services:
  frontend:

    image: nhothoang/water-app-frontend:latest
    container_name: nextjs_frontend
    restart: unless-stopped
    ports:
      - "3001:3000"
    networks:
      - app_network

networks:
  app_network:
    driver: bridge
    external: true
